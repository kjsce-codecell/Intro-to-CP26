// Logical
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a = 10, b = 20;

    cout << ((a<20) && (b<20)) << "\n";  // logical AND
    cout << ((a<20) || (b<20)) << "\n";  // logical OR
    cout << (!(a<20))     << "\n";  // logical NOT
}