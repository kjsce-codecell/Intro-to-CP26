#include <bits/stdc++.h>
using namespace std;

int main(){

    int arr[] = {10, 20, 30, 40, 50};

    // Constant Time O(1)
    cout << "First Element : " << arr[0] <<"\n";  
    // Always takes the same time
    return 0;
}