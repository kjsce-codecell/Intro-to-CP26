// Logical
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x = 7;

    cout << ((x<5) && (x<10))  << "\n";  // logical AND
    cout << ((x<5) || (x<4))   << "\n";  // logical OR
    cout << (!(x<5 && (x<10))  << "\n";  // logical NOT
}
